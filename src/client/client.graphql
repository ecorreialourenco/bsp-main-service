type Client {
  id: String!
  name: String!
  address: String!
  type: ClientType!
  city: String
  state: String
  country: String
  zipCode: String
  clientNr: Int!
  contacts: [ClientContact]
  companyClientRoleId: String!
  role: ClientRole
}

type ClientResponsePaginated {
  clients: [Client]
  totalCount: Int
}

input ClientInput {
  name: String!
  address: String!
  type: ClientType!
  city: String!
  state: String!
  country: String!
  zipCode: String!
  companyId: String!
  companyClientRoleId: String!
}

input ClientListInput {
  companyId: String!
  search: String
  limit: Int
  offset: Int
  cursor: Int
  sortBy: String
  sortOrder: String
}

enum ClientType {
  Personal
  Company
}

type Query {
  getClient(id: String!): Client
  listClients(input: ClientListInput): ClientResponsePaginated
  listClientTypes: [String]
}

type Mutation {
  createClient(input: ClientInput!): Client
  updateClient(id: String!, input: ClientInput!): Client
  removeClient(id: String!, forceDelete: Boolean): String
  restoreClient(id: String!): String
}

type ClientRole {
  id: String!
  name: String
  company: Company
  clients: [Client]
  discounts: [ClientDiscount]
}

type ClientRolesPaginated {
  roles: [ClientRole]
  totalCount: Int
}

input ClientRoleInput {
  name: String!
  companyId: String!
}

input ClientRolesListInput {
  companyId: String!
  limit: Int
  offset: Int
  cursor: Int
  sortBy: String
  sortOrder: String
}

type Query {
  getClientRole(id: String!): ClientRole
  listClientRoles(input: ClientRolesListInput): ClientRolesPaginated
}

type Mutation {
  createClientRole(input: ClientRoleInput!): ClientRole
  updateClientRole(id: String!, input: ClientRoleInput!): ClientRole
  deleteClientRole(id: String!, forceDelete: Boolean): String
  restoreClientRole(id: String!): String
}

type Provider {
  id: String!
  name: String
  address: String
  city: String
  state: String
  country: String
  zipCode: String
  providerNr: Int
  contacts: [ProviderContact]
  #products: [Product]
  productsCount: Int
}

type ProviderListPaginated {
  providers: [Provider]
  totalCount: Int
}

input ProviderInput {
  name: String!
  website: String!
  address: String!
  city: String!
  state: String!
  country: String!
  zipCode: String!
  companyId: String!
}

input ProviderListInput {
  companyId: String!
  search: String
  limit: Int
  offset: Int
  cursor: Int
  sortBy: String
  sortOrder: String
}

type Query {
  getProvider(id: String!): Provider
  listProviders(input: ProviderListInput): ProviderListPaginated
}

type Mutation {
  createProvider(input: ProviderInput!): Provider
  updateProvider(id: String!, input: ProviderInput!): Provider
  removeProvider(id: String!, forceDelete: Boolean): String
  restoreProvider(id: String!): String
}

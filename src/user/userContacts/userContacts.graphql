type UserContact {
  id: Int!
  contact: String!
  user: User
  type: ContactType!
}

input UserContactInput {
  contact: String!
  type: ContactType!
  userId: Int!
}

enum ContactType {
  Email
  Fax
  Mobile
  Phone
}

type Query {
  listContactTypes: [String]
}

type Mutation {
  createUserContact(input: UserContactInput!): UserContact
  updateUserContact(id: Int!, input: UserContactInput!): UserContact
  removeUserContact(id: Int!): String
}

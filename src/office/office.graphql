type Office {
  id: String
  name: String
  address: String
  city: String
  state: String
  country: String
  zipCode: String
  company: Company
  #stock: [ProductStock]
}

input OfficeInput {
  name: String!
  address: String!
  city: String!
  state: String!
  country: String!
  zipCode: String!
  companyId: String!
}

input OfficeListInput {
  companyId: String!
  limit: Int
  offset: Int
  cursor: Int
  sortBy: String
  sortOrder: String
}

type OfficeResponse {
  office: Office
  status: Int
}

type OfficeResponsePaginated {
  offices: [Office]
  totalCount: Int
}

type Query {
  getOffice(Id: String): Office
  listOffices(input: OfficeListInput): OfficeResponsePaginated
}

type Mutation {
  createOffice(input: OfficeInput): OfficeResponse
  updateOffice(id: String, input: OfficeInput): OfficeResponse
  deleteOffice(id: String, forceDelete: Boolean): String
  restoreOffice(id: String): String
}
